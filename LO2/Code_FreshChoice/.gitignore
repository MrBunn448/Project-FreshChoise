#Dependencies

#Exclude all project dependencies in client/ and server/
node_modules/              

#Build Artifacts

#Exclude frontend build files (Vite/React output)
/client/dist               
/server/dist               
#Exclude backend build files (if compiled)
#Credentials & Sensitive Config

.env
.env.local

#Operating System / IDE / Logs
.DS_Store                  # macOS system files
.log                      # Exclude all log files
npm-debug.log # Debug logs generated by npm
yarn.lock                  # Yarn dependency lock file